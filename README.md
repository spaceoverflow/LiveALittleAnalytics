# LiveALittleAnalytics

## Fetcher

Gebruik `./install-tg.sh` om de `telegram-cli` tool te installeren op debian systemen.
Vervolgens kan je met `./fetch {CHAT NAAM}` de history van `{CHAT}` opvragen.